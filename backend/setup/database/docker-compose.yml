version: '3.9'

services:
  postgres:
    image: postgres:15
    container_name: team-communication-db
    environment:
      POSTGRES_USER: communicator
      POSTGRES_PASSWORD: sAfe_Pass5ord
      POSTGRES_DB: team-communication-db
    ports:
      - "5430:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
